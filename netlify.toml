[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "nft"

# Sonnet on Medium/Hard needs more headroom; Haiku on Easy is fast
[functions."generate-puzzle"]
  timeout = 26
